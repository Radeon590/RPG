using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HorseRiding : MonoBehaviour
{
 


   /* void Start()
   {
       HorseTr = GameObject.Find("HorseTrigger");
        _characterController = GetComponent<CharacterController>();
        isRide = HorseTr.GetComponent<HorseSitting>().isRide;

    }


    void FixedUpdate()
    {
 

        if (isRide == true)
        {
            if (Input.GetKey(KeyCode.W))
            {
                transform.Translate(Vector3.forward * HorseSpeed);
            }

            if (Input.GetKey(KeyCode.S))
            {
                transform.Translate(Vector3.back * HorseSpeed);
            }



           hero.transform.position = new Vector3(transform.position.x, transform.position.y + 1, transform.position.z);
            hero.transform.rotation = this.transform.rotation;

            if (Input.GetKey(KeyCode.LeftShift))
            {
                HorseSpeed = HorseRunSpeed;
            }
 
        }


    }*/

}
